# Project tool versions and common tasks for local development.
# Personal overrides: create mise.local.toml (gitignored).

[tools]
node = "26"
pnpm = "11.16.0"

[tasks.install]
description = "Install pnpm dependencies"
run = "pnpm install"

[tasks.build]
description = "Prebuild extension sources to dist/ with tsdown"
run = "pnpm run build"

[tasks.test]
description = "Run the Vitest suite"
run = "pnpm test"

[tasks."test:watch"]
description = "Run Vitest in watch mode"
run = "pnpm run test:watch"

[tasks."test:coverage"]
description = "Run tests with coverage"
run = "pnpm run test:coverage"

[tasks."test:oauth-provider"]
description = "Run OAuth provider node:test suite"
run = "pnpm run test:oauth-provider"

[tasks."bench:import"]
description = "Benchmark cold-start import cost"
run = "pnpm run bench:import"

[tasks.lint]
description = "Lint with oxlint"
run = "pnpm run lint"

[tasks."lint:fix"]
description = "Lint with oxlint and apply safe fixes"
run = "pnpm run lint:fix"

[tasks.fmt]
description = "Format with oxfmt"
run = "pnpm run fmt"

[tasks."fmt:check"]
description = "Check formatting with oxfmt"
run = "pnpm run fmt:check"
